# Backend base URL, no trailing slash. Read at BUILD time: redeploy after changing it.
# Defaults to http://localhost:5000 in `npm run dev` when unset.
VITE_BASEURL=https://ai-website-backend-yhbg.onrender.com
